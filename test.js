function imgWrapper (str) {
  let stringArray = str.split(" ");
  let imgUrlRegEx = new RegExp("/\.(gif|jpg|jpeg|tiff|png)$/i");
}

let imgUrlRegEx = new RegExp("/\.(gif|jpg|jpeg|tiff|png)$/i");
console.log(imgUrlRegEx.test("cat.jpg"));